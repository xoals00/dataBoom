<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.project.projectFinal.dao.GraphDao">

	<select id="itemGraphLine">
	
		select * from Top5ItemPerLineT where line_graph = #{line_graph}
	
	</select>

</mapper>